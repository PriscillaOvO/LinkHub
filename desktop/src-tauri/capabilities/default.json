{
  "identifier": "default",
  "description": "Default capabilities for LinkHub",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
